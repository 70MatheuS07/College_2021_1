#include <stdio.h>

int main () {

    int qtd, loop, pares=0, impares=0, numMaior=0, numMenor=1, num, soma=0;
    float media;

    scanf ("%d", &qtd);
    break;
    
    loop = loop + 1;

    while (loop <= qtd) {

       scanf ("%d", &num);

        loop++;
    }

    return 0;
}